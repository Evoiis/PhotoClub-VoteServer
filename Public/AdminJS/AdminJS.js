socket.on('connect', function(){
    socketid = socket.id;

    //socket.emit('projectini', socketid);
    

    //socket.on('', picture);

});

//socket.emit('');

function change_photo(forward){
    
}